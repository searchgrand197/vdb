import { Box, Typography } from '@mui/material';

/** Placeholder — replace with calendar / booking UI. */
export function AppointmentsPage() {
  return (
    <Box>
      <Typography variant="h5" sx={{ mb: 2 }}>
        Appointments
      </Typography>
      <Typography variant="body2" color="text.secondary">
        Module scaffold — connect to scheduling APIs and resource views.
      </Typography>
    </Box>
  );
}
